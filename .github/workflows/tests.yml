name: Build & Test

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-test:
    name: Build & Test
    runs-on: windows-latest   # WPF targets net9.0-windows
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: '9.0.x'

      - name: Restore
        run: dotnet restore FrameWrite.sln

      - name: Build (Release)
        run: dotnet build FrameWrite.sln --no-restore --configuration Release

      - name: Test (Release)
        run: dotnet test FrameWrite.sln --no-build --configuration Release --verbosity normal
